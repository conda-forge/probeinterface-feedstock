{% set name = "probeinterface" %}
{% set version = "0.2.26" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/probeinterface-{{ version }}.tar.gz
  sha256: f47b07f6352aa956632ed0c250ac364de446dc44b2234145ca69a00a99ebf8b8

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=62.0
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - packaging

test:
  imports:
    - probeinterface
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/SpikeInterface/probeinterface
  summary: Python package to handle probe layout, geometry and wiring to device.
  license: MIT
  license_file: LICENSE
  doc_url: https://probeinterface.readthedocs.io/
  dev_url: https://github.com/SpikeInterface/probeinterface

extra:
  recipe-maintainers:
    - dhmjhu
